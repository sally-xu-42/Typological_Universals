# Create separate virtual environments for data processing

Make sure you are on root directory.
If you have activated a virtual environment, run the following command:

```
deactivate
rm -r <env_folder_name>
```

